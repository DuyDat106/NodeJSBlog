<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <h6 class="mt-4">Bạn đã đăng ký khóa học này</h6>
            <button class="btn btn-primary">Học ngay</button>
            <ul>
                <li>{{course.level}}</li>
            </ul>
        </div>
        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <iframe width="900" height="500" src="https://www.youtube.com/embed/{{course.videoId}}"
                title="YouTube video player" frameborder="0"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                allowfullscreen>
            </iframe>
            <h4>{{course.description}}</h4>
        </div>
    </div>
</div>